# GEMINI_API_KEY: Required for Gemini AI Smart Import calls
GEMINI_API_KEY=

# NODE_ENV: Environment mode (development / production)
NODE_ENV=production

# PORT: Server port (defaults to 3000 if not provided by host)
PORT=3000

